package com.humber.labs;

@FunctionalInterface
public interface CustomFunction {
	// Define a functional interface with a single abstract method
	    int performOperation(int a, String b);
}

